resource "google_compute_instance" "ubuntu-dmurga" {
  boot_disk {
    auto_delete = true
    device_name = "ubuntu-dmurga"

    initialize_params {
      image = "projects/ubuntu-os-pro-cloud/global/images/ubuntu-pro-2004-focal-v20230614"
      size  = 10
      type  = "pd-balanced"
    }

    mode = "READ_WRITE"
  }

  can_ip_forward      = false
  deletion_protection = false
  enable_display      = false

  labels = {
    goog-ec-src = "vm_add-tf"
  }

  machine_type = "e2-medium"
  name         = "ubuntu-dmurga"

  network_interface {
    access_config {
      network_tier = "PREMIUM"
    }

    subnetwork = "projects/rancher-support-01/regions/europe-southwest1/subnetworks/default"
  }

  scheduling {
    automatic_restart   = true
    on_host_maintenance = "MIGRATE"
    preemptible         = false
    provisioning_model  = "STANDARD"
  }

  service_account {
    email  = "851496582062-compute@developer.gserviceaccount.com"
    scopes = ["https://www.googleapis.com/auth/devstorage.read_only", "https://www.googleapis.com/auth/logging.write", "https://www.googleapis.com/auth/monitoring.write", "https://www.googleapis.com/auth/service.management.readonly", "https://www.googleapis.com/auth/servicecontrol", "https://www.googleapis.com/auth/trace.append"]
  }

  shielded_instance_config {
    enable_integrity_monitoring = true
    enable_secure_boot          = false
    enable_vtpm                 = true
  }

  zone = "europe-southwest1-a"
}
